# Public URL of the main Sprint Retro app (used by every CTA/Login button on the landing page).
# Baked in at build time (Next.js public env vars are compiled into the client bundle).
NEXT_PUBLIC_APP_URL=http://localhost:3000
